# Copy to .env locally; never commit real secrets.
PAYLOAD_SECRET=replace-with-at-least-32-random-bytes
SITE_URL=http://localhost:3000
# CMS_REMOTE=1 is only for explicitly running migrations against production.
